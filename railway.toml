[build]
builder = "nixpacks"

[deploy]
startCommand = "/opt/venv/bin/python server.py"
healthcheckPath = "/api/health"
healthcheckTimeout = 100
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
